import styles from "./Footer.module.css"

const Footer = () => {
    return(
        <div className={styles.foot}>
            <p>Music first. Always.</p>
        </div>
    )
}
export default Footer;